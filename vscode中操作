————————dos命令行：
dos命令进入golang>bin
（1）查看版本：go version
     也可以不用进入此文件中（即想在任意目录下执行go命令），但需要将该命令写入环境变量path中
（2）对源文件进行编译：
    1go build 文件名
     如在下面目录结构下
     然后会生成对应文件的exe，进而在这个命令行中输入文件名.exe,即可在命令窗口中得到执行
    2go run 文件名直接编译、执行
————————基本目录结构：
总——src——gocode（写代码包的地方）————项目1,2————main函数等

  
