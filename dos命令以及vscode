即命令行中的命令（没有大小写之分）
  （1）切换盘符：要求换的盘：
           举例：C：
  （2）显示当前目录详细信息：dir
  （3）改变目录（即还是此盘，但是换盘中不同文件目录）
  （4）cd后跟目录  跟着目录名时如果懒得写完整名字，可以写一部分用tab键
      cd..是上一层目录
  （5）清屏命令：cls
  （6）创建目录：md 目录名
       删除目录：re 目录名
       删除文件：del 文件名（有后缀）
                del 文件夹名 则删除文件夹中的所有文件，不删除文件夹       
  （7）copy 文件名 复制到的目录名\ 复制后的名字
————————dos命令行：
dos命令进入golang>bin
（1）查看版本：go version
     也可以不用进入此文件中（即想在任意目录下执行go命令），但需要将该命令写入环境变量path中
（2）对源文件进行编译：
    1go build 文件名
     如在下面目录结构下
     然后会生成对应文件的exe，进而在这个命令行中输入文件名.exe,即可在命令窗口中得到执行
    2go run 文件名直接编译、执行
————————基本目录结构：
总——src——gocode（写代码包的地方）————项目1,2————main函数等

  
